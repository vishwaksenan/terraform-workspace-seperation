# Terraform Workspace Dev/Prod Seperation

High level goal is to run a seperate EC2 instance for dev and prod where in dev the AWS region should be `eu-west-2` and prod should run in region `ap-southeast-1`.